# Optional: force a specific backend URL instead of auto-detecting it from
# the page's hostname (src/config/api.js). Leave unset for normal use — it
# lets the same build work from localhost, a LAN IP, or any other host.
# VITE_API_URL=http://192.168.88.5:5000/api
