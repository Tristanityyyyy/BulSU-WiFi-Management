# Base URL the frontend uses to reach the backend API.
# Use the machine's LAN IP (not localhost) if this frontend will be
# loaded from other devices, e.g. phones going through the captive portal.
VITE_API_URL=http://localhost:5000/api
