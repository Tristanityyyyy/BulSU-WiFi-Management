// Canonical role display labels, shared by every admin screen that renders a
// role (feedback table, network settings, etc.) so they can't drift apart.
export const ROLE_LABELS = {
  student: "Student",
  faculty: "Faculty",
  staff: "Staff",
  guest: "Guest",
  admin: "Admin",
  unknown: "Unknown",
};
